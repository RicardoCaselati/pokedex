import styles from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={styles.containerFooter}>
      <p>Desenvolvido por Ricardo Caselati</p>
    </footer>
  );
};

export default Footer;
